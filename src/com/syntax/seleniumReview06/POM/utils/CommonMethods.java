package com.syntax.seleniumReview06.POM.utils;

import com.syntax.seleniumReview06.POM.testBase.BaseClass;
import org.openqa.selenium.NoSuchFrameException;

import static java.awt.SystemColor.text;

public class CommonMethods extends BaseClass {
    //   public void clickOnElement() {
    /**
     * this method will send text to a given element
     * @param element
     * @param text
     */
   /*     public void sendText(WebElementelement, String text) {
            element.clear();
            element.sendKeys(text);
        }


    */

    /**
     * this method will switch to a frame by index
     * @param index
     */
     /*   public void switchToFrame(int index){
            try {
                driver.switchTo().frame(index);
            } catch (NoSuchFrameException e) {
                e.printStackTrace();
            }
        }

    }
    */
}

